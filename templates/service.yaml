apiVersion: v1
kind: Service
metadata:
  name: {{ include "capstone-chart-conf-openvas-scanner.fullname" . }}
  labels:
    {{- include "capstone-chart-conf-openvas-scanner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capstone-chart-conf-openvas-scanner.selectorLabels" . | nindent 4 }}
